! function() {
    "use strict";
    var i = Math.max(document.documentElement.clientWidth || 0, window.innerWidth || 0),
        e = document.querySelector("#mainNav");
    if (e && i > 992) {
        var n = e.offsetHeight,
            t = window.pageYOffset;
        window.addEventListener("scroll", (function() {
            var i = window.pageYOffset;
            i < t ? i > 0 && e.classList.contains("is-fixed") ? e.classList.add("is-visible") : e.classList.remove("is-visible", "is-fixed") : i > t && (e.classList.remove("is-visible"), i > n && !e.classList.contains("is-fixed") && e.classList.add("is-fixed")), t = i
        }))
    }
}(), document.addEventListener("DOMContentLoaded", (function() { new Swiper(".swiper-container", { loop: !0, pagination: { el: ".swiper-pagination", clickable: !0 }, paginationClickable: !0, navigation: { nextEl: ".swiper-button-next", prevEl: ".swiper-button-prev" } }) }));

function openForm() {
    document.getElementById("myForm").style.display = "block";
}

function closeForm() {
    document.getElementById("myForm").style.display = "none";
}



/* Comienza código de mapas */
var map, marker;

function GetMap() {
    //Initialize a map instance.
    map = new atlas.Map('myMap', {
        center: [-110, 45],
        view: 'Auto',

        //Alternatively, use an Azure Maps key. Get an Azure Maps key at https://azure.com/maps. NOTE: The primary key should be used as the key.
        authType: 'subscriptionKey',
        subscriptionKey: 'giXFaoH8SZlfxZly3Pp8QDuI_mk2uwvDHgH5IoTa6p0'

    });

    //Wait until the map resources are ready.
    map.events.add('ready', function() {
        //Create a HTML marker and add it to the map.
        marker = new atlas.HtmlMarker({
            htmlContent: '<div class="pulseIcon"></div>',
            position: [-117.02266062655534, 32.53750131024909]
        });

        map.markers.add(marker);
        //Create a HTML marker and add it to the map.
        marker = new atlas.HtmlMarker({
            htmlContent: '<div class="pulseIcon"></div>',
            position: [-116.99261791240325, 32.51029264343046]
        });

        map.markers.add(marker);
        //Create a HTML marker and add it to the map.
        marker = new atlas.HtmlMarker({
            htmlContent: '<div class="pulseIcon"></div>',
            position: [-116.90400838230332, 32.48387947220384]
        });

        map.markers.add(marker);
    });
}